# 本地 Docker 开发环境配置
# 使用 --spring.profiles.active=local 激活此配置
# 覆盖默认的 192.168.109.128 虚拟机地址为 localhost，连接本地 Docker 容器

spring:
  data:
    redis:
      host: localhost
      port: 6379
      database: 0
      password: dchw0415
      timeout: 5000ms
  datasource:
    url: jdbc:mysql://localhost:3307/lease?useUnicode=true&characterEncoding=utf-8&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=GMT%2b8
    username: root
    password: Dcw.0415

minio:
  endpoint: http://localhost:9000
  access-key: minioadmin
  secret-key: minioadmin
  bucket-name: lease
