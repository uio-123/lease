server:
  port: 8081

spring:
  data:
    redis:
      host: 192.168.109.128
      port: 6379
      database: 0
      password: dchw0415
      timeout: 5000ms  # 建议加上超时时间
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    url: jdbc:mysql://192.168.109.128:3306/lease?useUnicode=true&characterEncoding=utf-8&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=GMT%2b8
    username: root
    password: Dcw.0415
    hikari:
      connection-test-query: SELECT 1 # 自动检测连接
      connection-timeout: 60000 #数据库连接超时时间,默认30秒
      idle-timeout: 500000 #空闲连接存活最大时间，默认600000（10分钟）
      max-lifetime: 540000 #此属性控制池中连接的最长生命周期，值0表示无限生命周期，默认1800000即30分钟
      maximum-pool-size: 12 #连接池最大连接数，默认是10
      minimum-idle: 10 #最小空闲连接数量
      pool-name: SPHHikariPool # 连接池名称
  jackson:
    time-zone: GMT+8

#用于打印框架生成的sql语句，便于调试
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

minio:
  endpoint: http://192.168.109.128:9000
  access-key: minioadmin
  secret-key: minioadmin
  bucket-name: lease

springdoc:
  default-flat-param-object: true

# 短信服务配置
# mock=true 时使用模拟模式（验证码打印到控制台），不需要配置密钥
# mock=false 时使用阿里云短信发送，需取消下方注释并填入真实值
sms:
  mock: true
  #aliyun:
  #  access-key-id: your-access-key-id
  #  access-key-secret: your-access-key-secret
  #  sign-name: your-sign-name
  #  template-code: SMS_XXXXX